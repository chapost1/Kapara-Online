import { HighlightSearch } from './hightlight-pipe.pipe';

describe('PipesPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSearch();
    expect(pipe).toBeTruthy();
  });
});
