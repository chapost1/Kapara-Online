import { CurrentImagePipe } from './current-image.pipe';

describe('CurrentImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentImagePipe();
    expect(pipe).toBeTruthy();
  });
});
