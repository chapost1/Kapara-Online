import { HeIlCurrencyFormatPipe } from './currency-format.pipe';

describe('CurrencyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HeIlCurrencyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
